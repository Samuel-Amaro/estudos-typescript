/**
 * * MATRIZES(ARRAY)
 * 
 * Para especificar o tipo de um array como [1, 2, 3], você pode usar a sintaxe number[]; esta sintaxe funciona para qualquer tipo (por exemplo, string[] é uma array de strings e assim por diante). Você também pode ver isso escrito como Array<number>, o que significa a mesma coisa. Aprenderemos mais sobre a sintaxe T<U> quando abordarmos os genéricos.
 * 
 * Observe que [number] é uma coisa diferente; consulte a seção sobre Tuplas.
*/