/**
 * * OBJECT(OBJETO)
 * 
 * O tipo especial object refere-se a qualquer valor que não seja primitivo ( string, number, bigint, boolean, symbol, null ou undefined). Isso é diferente do tipo de objeto vazio { } e também diferente do tipo global Object. É muito provável que você nunca use Object. 
 *  
 * object não é Object. Use sempre object!
 * 
 * Observe que em JavaScript, os valores de função são objetos: eles têm propriedades, têm Object.prototype em sua cadeia de protótipos, são instanceof Object, você pode chamá los Object.keys- e assim por diante. Por esse motivo, os tipos de função são considerados objects no TypeScript.
*/